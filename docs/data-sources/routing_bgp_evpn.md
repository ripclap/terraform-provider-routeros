# routeros_routing_bgp_evpn (Data Source)


## Example Usage
```terraform
# Entries in the menu, narrowed by any attribute it returns.
# Omit `filter` to read all of them.
data "routeros_routing_bgp_evpn" "example" {
  filter = {
    name = "example"
  }
}

output "routing_bgp_evpn" {
  value = data.routeros_routing_bgp_evpn.example.entries[*].name
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `___id___` (Number) <em>Resource ID type (.id / name). This is an internal service field, setting a value is not required.</em>
- `___path___` (String) <em>Resource path for CRUD operations. This is an internal service field, setting a value is not required.</em>
- `filter` (Map of String) Additional request filtering options.

### Read-Only

- `entries` (List of Object) Entries read from `/routing/bgp/evpn`, narrowed by `filter`. (see [below for nested schema](#nestedatt--entries))
- `id` (String) The ID of this resource.

<a id="nestedatt--entries"></a>
### Nested Schema for `entries`

Read-Only:

- `comment` (String)
- `disabled` (Boolean)
- `export` (List of Object) (see [below for nested schema](#nestedobjatt--entries--export))
- `id` (String)
- `import` (List of Object) (see [below for nested schema](#nestedobjatt--entries--import))
- `instance` (String)
- `name` (String)
- `rd` (String)
- `vni` (Number)
- `vrf` (String)

<a id="nestedobjatt--entries--export"></a>
### Nested Schema for `entries.export`

Read-Only:

- `route_targets` (Set of String)


<a id="nestedobjatt--entries--import"></a>
### Nested Schema for `entries.import`

Read-Only:

- `route_targets` (Set of String)



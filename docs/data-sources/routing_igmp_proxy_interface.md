# routeros_routing_igmp_proxy_interface (Data Source)


## Example Usage
```terraform
# Entries in the menu, narrowed by any attribute it returns.
# Omit `filter` to read all of them.
data "routeros_routing_igmp_proxy_interface" "example" {
  filter = {
    interface = "ether1"
  }
}

output "routing_igmp_proxy_interface" {
  value = data.routeros_routing_igmp_proxy_interface.example.entries[*].id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `___id___` (Number) <em>Resource ID type (.id / name). This is an internal service field, setting a value is not required.</em>
- `___path___` (String) <em>Resource path for CRUD operations. This is an internal service field, setting a value is not required.</em>
- `filter` (Map of String) Additional request filtering options.

### Read-Only

- `entries` (List of Object) Entries read from `/routing/igmp-proxy/interface`, narrowed by `filter`. (see [below for nested schema](#nestedatt--entries))
- `id` (String) The ID of this resource.

<a id="nestedatt--entries"></a>
### Nested Schema for `entries`

Read-Only:

- `alternative_subnets` (Set of String)
- `comment` (String)
- `disabled` (Boolean)
- `id` (String)
- `inactive` (Boolean)
- `interface` (String)
- `threshold` (Number)
- `upstream` (Boolean)



# routeros_user_manager_limitation (Data Source)


## Example Usage
```terraform
# Entries in the menu, narrowed by any attribute it returns.
# Omit `filter` to read all of them.
data "routeros_user_manager_limitation" "example" {
  filter = {
    name = "example"
  }
}

output "user_manager_limitation" {
  value = data.routeros_user_manager_limitation.example.entries[*].name
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `___id___` (Number) <em>Resource ID type (.id / name). This is an internal service field, setting a value is not required.</em>
- `___path___` (String) <em>Resource path for CRUD operations. This is an internal service field, setting a value is not required.</em>
- `filter` (Map of String) Additional request filtering options.

### Read-Only

- `entries` (List of Object) Entries read from `/user-manager/limitation`, narrowed by `filter`. (see [below for nested schema](#nestedatt--entries))
- `id` (String) The ID of this resource.

<a id="nestedatt--entries"></a>
### Nested Schema for `entries`

Read-Only:

- `download_limit` (Number)
- `id` (String)
- `name` (String)
- `rate_limit_burst_rx` (Number)
- `rate_limit_burst_threshold_rx` (Number)
- `rate_limit_burst_threshold_tx` (Number)
- `rate_limit_burst_time_rx` (String)
- `rate_limit_burst_time_tx` (String)
- `rate_limit_burst_tx` (Number)
- `rate_limit_min_rx` (Number)
- `rate_limit_min_tx` (Number)
- `rate_limit_priority` (Number)
- `rate_limit_rx` (Number)
- `rate_limit_tx` (Number)
- `reset_counters_interval` (String)
- `reset_counters_start_time` (String)
- `transfer_limit` (Number)
- `upload_limit` (Number)
- `uptime_limit` (String)


